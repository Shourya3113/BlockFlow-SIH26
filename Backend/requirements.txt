fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0
scipy>=1.10.0
numpy>=1.24.0
pandas>=2.0.0
requests>=2.28.0

# Imported by Backend/ai_engine (ml_degradation_model, multi_agent_system).
# Without these the FastAPI gateway cannot import, so a guidebook-fresh
# `pip install -r Backend/requirements.txt` would not boot the service.
scikit-learn>=1.3.0
torch>=2.1.0
